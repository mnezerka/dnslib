DNS Protocol Library

Simple c++ library for encoding and decoding DNS wire-format packets.

Checked with valgrind tool (valgrind --leak-check=full ./unittests)

Linted with cppcheck (cppcheck --enable=all *cpp)

